//Imports of dependencies
import React, { FunctionComponent } from 'react';
import { Grid } from '@material-ui/core';

const ErrorNotFound: FunctionComponent = () => {
	return (
		<Grid item xs={12}>
			<div>Error Not Found 404</div>
		</Grid>
	);
};

export default ErrorNotFound;
